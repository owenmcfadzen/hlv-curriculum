[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Railway sets PORT automatically; server.mjs reads it. No env vars required for
# the static site. If you add a custom domain, configure it in the Railway UI
# under Settings → Networking.
