.DS_Store
node_modules/
*.log
.claude/
.env
.env.local
/tmp/
*.swp
.idea/
.vscode/

# Raw source materials — local only, never committed
sources/decks/
sources/notion/
sources/transcripts/
sources/worksheets/
!sources/decks/.gitkeep
!sources/notion/.gitkeep
!sources/transcripts/.gitkeep
!sources/worksheets/.gitkeep

# Derivative data exports — regenerable via tools/export-data.mjs
data/schedule.json
data/activities.json
data/days.json

# Private-only files — must not land in public repo (see ARCHITECTURE.md file-layout table)
PROJECT.md
HANDOFF.md

# Local persistent state — written by server.mjs when running locally
overrides.json
